<!DOCTYPE html>
<html lang="en">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Account</title>
    <style>
        /* Login */

        @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');

        * {
            box-sizing: border-box;
            margin: 0;
            font-family: 'Poppins', sans-serif;
        }

        html {
            background: linear-gradient(149deg, rgba(185, 168, 255, 1) 0%, rgba(214, 234, 255, 1) 48%, rgba(255, 225, 124, 1) 100%);
            height: 100%;
        }

        body {

            margin: 30px;
        }

        .body-container {
            width: 80%;
            display: flex;
            flex-direction: row;
            box-shadow: 0px 0px 45px #a8a8a8;
            border-radius: 30px;
            height: 93vh;
            margin: auto;
        }

        .container {
            width: 80%;
            display: flex;
            flex-direction: row;
            box-shadow: 0px 0px 45px #a8a8a8;
            border-radius: 30px;
            height: 93vh;
            margin: auto;
        }

        .desktop {
            width: 50%;
            background-color: #ffffff;
        }

        #loginCover {
            background-color: #ffe17c;
        }

        #signupCover {
            background-color: #a298db;
            color: #FFFFFF;
        }

        .cover-container,
        .form-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100%;
            justify-content: center;
        }

        input.input-box:first-child {
            margin-top: 30px;
        }

        input.input-box {
            width: 350px;
            display: block;
            margin-bottom: 30px;
            padding: 15px;
            font-size: 18px;
            border: 2px solid #1d1d1d;
            border-radius: 20px;
            outline: none;
            transition: box-shadow 0.5s;
        }

        .form input:focus {
            box-shadow: 5px 5px 15px #a8a8a8;
        }

        .switch-btn {
            font-size: 20px;
            padding: 10px 55px;
            margin: 30px 0px;
            color: #ffffff;
            font-weight: bold;
            letter-spacing: 5px;
            border: none;
            border-radius: 10px;
            box-shadow: 5px 5px 20px #797979;
            transition: box-shadow 0.5s;
            outline: none;
        }


        #loginCover .switch-btn {
            background-color: #a298db;
        }

        #signupCover .switch-btn {
            background-color: #e8c757;
        }

        input[type=button],
        input[type=submit] {
            margin: auto;
            padding: 10px;
            background-color: #71b9ff;
            color: #fff;
            border: none;
            font-size: 22px;
            border-radius: 10px;
            transition: box-shadow 0.5s;
            outline: none;
            cursor: pointer;
            width: 350px;
        }

        .mobile {
            display: none;
            background-color: coral;
        }

        .hide {
            display: none;
        }

        /* Log In Section */
        #login-section {
            border-top-right-radius: 0;
            border-bottom-right-radius: 0;
        }

        #login-section h1 {
            color: black;
            font-size: 50px;
        }

        /* Sign Up Section */
        #signup-section {
            border-top-left-radius: 0;
            border-bottom-left-radius: 0;
        }

        #signup-section h1 {
            font-size: 50px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        @media (max-width: 768px) {
            .desktop {
                display: none;
            }

            .mobile {
                display: flex;
                width: 100%;
                height: 500px;
                background-image: url('pastel-bg.jpg');
                background-size: cover;
                background-repeat: no-repeat;
            }

            .mobile p {
                font-size: 25px;
            }
        }

        @media (max-width: 480px) {
            .mobile {
                background-color: red !important;
            }

            .mobile p {
                font-size: 15px;
            }
        }
    </style>
    {{!--
    <link rel="stylesheet" href="style.css"> --}}
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"
        integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa"
        crossorigin="anonymous"></script>
</head>

<body>

    <div stype="text-align: center" class="alert alert-{{message.type}}"></div>

    <div class="container">
        <!--LOGIN SECTION-->
        <section id="login-section" class="desktop">
            <div class="cover-container hide" id="loginCover">
                <h1>Welcome !</h1>
                <h3>Already have a Company Account?</h3>
                <button type="button" class="switch-btn" onclick="toggleLoginSignup('login')">Login</button>
            </div>
            <div class="form-container" id="loginForm">
                <h1>Login</h1>
                <form action="/login" method="POST">
                    <input type="text" name="useremail" class="input-box" placeholder="User Email">
                    <input type="password" name="password" class="input-box" placeholder="Password">
                    <input type="submit" name="login-btn" class="btn" value="Login">
                </form>
            </div>
        </section>

        <!--SIGN UP SECTION-->

        <section id="signup-section" class="desktop">
            <div class="cover-container" id="signupCover">
                <h1>Welcome!</h1>
                <h3>Don't have a company account?</h3>
                <h3>Register your <u>company </u>and <u>start your FREE 30 day trial today.</u></h3>
                <!-- <button type="button" class="switch-btn" onclick="toggleLoginSignup('Get Started')"></button> -->
                <form action="/getStarted" method="post">
                    <input type="submit" name="signup-btn" class="btn" value="Get Started">
                </form>
            </div>
        </section>
    </div>

    <section class="mobile">
    </section>
    {{!--
    <script src="clientSide.js"></script> --}}
    <script src="index.js"></script>
</body>

</html>